{
  "framework": "nextjs",
  "installCommand": "pnpm install",
  "buildCommand": "bash ../../scripts/check-migration-safety.sh && prisma migrate deploy --schema=../../prisma/schema.prisma && prisma generate --schema=../../prisma/schema.prisma && pnpm run build",
  "crons": [
    {
      "path": "/api/cron/expire-sweep",
      "schedule": "0 3 * * *",
      "headers": {
        "Authorization": "Bearer ${CRON_SECRET}"
      }
    },
    {
      "path": "/api/cron/reconcile-cancellations",
      "schedule": "0 4 * * *",
      "headers": {
        "Authorization": "Bearer ${CRON_SECRET}"
      }
    }
  ]
}